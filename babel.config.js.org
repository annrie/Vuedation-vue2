// const MIN_BABEL_VERSION = 7

// module.exports = (api) => {
//   api.assertVersion(MIN_BABEL_VERSION)
//   api.cache(true)

const presets = [
  [
    '@babel/preset-env',
    {
      targets: {
        node: 'current',
      },
    },
  ],
]
module.exports = { presets }
